# v2 tool declaration change

Changed only the descriptions of `social_search` and `lookup`.

Hypothesis: when a later turn explicitly replaces Twitter/X with web search,
negative routing language in both competing tool declarations will prevent the
model from replaying the stale social intent while preserving the current topic.
